interface OldUser {
  email: string;
  firstName: string;
  lastName: string;
}

interface Session {
  token: string;
  accountType: string;
}

interface User {
  email: string;
  firstName: string;
  lastName: string;
  phoneNum: string;
  address: string;
}
